Building a CPP based project using CMake and adding submodules, custom libraries to it.

Steps to configure, build and run the executables on an Linux Environment.

* ./configure.sh
* ./build.sh
* ./run.sh